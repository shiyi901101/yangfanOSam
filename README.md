# 扬帆计划 · 运营中枢（扬帆 OS）

同路生物扬帆计划项目负责人的一站式工作台。单文件前端应用，零后端、零外部依赖，所有组件库随项目同源部署，可直接托管到 GitHub Pages 获得永久链接。

## 这个平台里有什么

- **① 学习**：PMP 教科书级项目管理学习系统（超详细 v5，含敏捷管理、国内外经典案例）。
- **② 提效**：8 个效率工具（成长报告生成、汇报 PPT、执行表、在线收集表、接龙、通知书/证书、短信话术等），支持搜索过滤，常用工具已钉到最前。
- **③ 解惑**：训战机器人，结合扬帆策略 + 真实数据 + 工具包，带新人快速上手、常见问题自查、直接出方案材料。
- **④ 看板**：全站数据驾驶舱，月份动态切换、指标排名、需要盯的站；支持各站自主上传月度数据。
- **⑤ 交流**：项目负责人论坛，按运营进程分板块，热门帖子轮播、发帖回帖可插图、支持搜索排序筛选。

## 目录结构

```
.
├── index.html                      # 入口页（自动跳转主平台）
├── 扬帆OS_综合平台v4.html          # 主平台（五个模块合一）
├── 扬帆OS_学习系统v5.html          # 学习系统（内嵌于主平台，也可独立打开）
├── 扬帆OS_成长报告生成器.html      # 学生成长报告生成器（独立打开）
├── dash_data.js                    # 看板数据（各站上报的月度汇总）
├── libs/
│   ├── pptxgen.bundle.js           # PPT 生成组件（同源部署，无 CDN）
│   └── xlsx.full.min.js            # Excel 解析组件（同源部署，无 CDN）
└── README.md
```

## 部署到 GitHub Pages（拿永久链接）

> 一次配置，链接永不变，浆站人每次打开都带本机记忆。

1. 在 GitHub 新建一个**空仓库**（仓库名随意，例如 `yangfan-os`）。
2. 进入本目录，初始化并推上去：
   ```bash
   git init
   git add -A
   git commit -m "扬帆OS 首版"
   git branch -M main
   git remote add origin https://github.com/<你的用户名>/<仓库名>.git
   git push -u origin main
   ```
3. 仓库 → **Settings → Pages** → Source 选 `Deploy from a branch` → Branch 选 `main` / 目录选 `/（root）` → Save。
4. 等 1 分钟左右，访问：`https://<你的用户名>.github.io/<仓库名>/`
   入口页会自动跳进主平台。

之后要更新内容：改完源文件 → 把新文件拷回本目录 → `git add -A && git commit -m "更新说明" && git push` 即可。

## 几个使用上的实话

- **登录密码**：默认 `yf2026`。登录态存在浏览器本机（localStorage，同域共享），属于"防君子不防小人"的静态页假登录。要做真权限（浆站只能改自己站、事业部看全部），得上后端。
- **数据存在本机**：论坛帖子、看板数据都只存在打开它的那台浏览器里。换设备/清缓存就没了。重要数据用主平台工具栏的「📦 全平台备份」导出 json 带走，到新设备用「📂 恢复数据」导入。
- **多站互通**：现在是各站各自一份数据。要让全集团浆站在一个论坛里互相发帖、共享看板，需要上后端数据库（如 Supabase / LeanCloud），这是下一步升级方向。
- **看板数据基准**：看板里的数是各站上报的月度汇总 + 半年度会议材料，不是实时数据，供运营参考。更新看板数据用主平台「📤 更新数据」上传解析。

## 技术约束

- 不引入任何外部 CDN，pptxgenjs / SheetJS 等组件库随项目同源部署（见 `libs/`）。
- 纯静态，可直接用任意静态托管（GitHub Pages / 任意对象存储 / 内网服务器）。
